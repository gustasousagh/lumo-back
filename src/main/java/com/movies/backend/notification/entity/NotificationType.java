package com.movies.backend.notification.entity;

/** Tipos de notificação que o app pode gerar. */
public enum NotificationType {
    FRIEND_REQUEST,
    FRIEND_ACCEPTED,
    ROOM_INVITE,
    COMMENT,
    GENERIC
}
